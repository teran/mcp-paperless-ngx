# Binaries
mcp-paperless-ngx
mcp-paperless-ngx.exe
*.out
*.test

# Go workspace
go.work
go.work.sum

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Dependency directories
vendor/

# Pre-built binaries from goreleaser (copied for Docker build)
mcp-paperless-ngx-linux-amd64
mcp-paperless-ngx-linux-arm64

# Build output
/dist/
/releases/

# Environment (sample kept)
.env
.env.local
*.env
# Root-only built binary; do NOT use a bare "server" pattern (it would also
# match the cmd/server source directory).
/server
